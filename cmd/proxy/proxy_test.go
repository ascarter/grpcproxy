package main_test

import "testing"

func TestProxy(t *testing.T) {
	testcases := []struct {
		desc string
	}{
		{
			desc: "",
		},
	}
	for _, tc := range testcases {
		t.Run(tc.desc, func(t *testing.T) {

		})
	}
}
